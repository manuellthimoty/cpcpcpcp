#include <bits/stdc++.h>
using namespace std;
using ll = long long;


void solve() {
    ll  n ; cin >> n ; 
    vector<ll> a(n);
    for (ll i = 0 ; i < n ; i++){
        cin >> a[i];
    }
    //definisiin banyak{k} sebagai banyaknya nilai k yang berakhir di indeks k.
    vector<ll> banyak1;
    vector<ll> banyak2;
    vector<ll> banyak3;

    for(ll i = 0 ; i < n ; i++){
        if(a[i] == 1) 
    }


}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}