#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n,x ; cin >> n >> x;
    vector<int> a(n+1,0);
    for(int i = 1 ; i<= n ; i++) cin >> a[i];
    for(int i = 1 ; i <= n ; i++){
        for(int j = 1 ; j < i ; j++){
            if(a[)
        }
    }

    return 0;
}